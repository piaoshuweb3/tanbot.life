# 摊博 TANBOT · 烟火节点 — 系统完整性审计报告

> **审计时间**: 2026-07-26 21:50 CST  
> **代码仓库**: [github.com/piaoshuweb3/tanbot.life](https://github.com/piaoshuweb3/tanbot.life)  
> **部署地址**: [tanbot-life.vercel.app](https://tanbot-life.vercel.app)  
> **域名**: tanbot.life (Cloudflare 托管)  
> **技术栈**: Next.js 16 + SQLite/libSQL + Tailwind CSS + shadcn/ui + DeepSeek API + Z.ai GLM VLM  

---

## 一、Git 同步状态

| 项目 | 状态 |
|------|------|
| 远程仓库 | `origin/main` @ github.com/piaoshuweb3/tanbot.life |
| 本地分支 | `main`，领先 origin 1 个 commit |
| 未暂存修改 | **100+ 文件**标记为 modified（疑似 CRLF/LF 换行符问题） |
| 未跟踪文件 | `src/lib/auth.ts`、`public/robots.txt`、`文书说明/` |
| 最后提交 | `1b1102c` — "fix: DeepSeek LLM + 首页摊车横幅填充 + 管理页面渲染修复" |

### ⚠️ 问题
1. **本地与远程不同步**：领先 1 个 commit，且 100+ 文件显示 modified，通常由 Windows Git `core.autocrlf` 设置引起
2. **敏感 .env 文件未忽略**：`.env*` 已在 `.gitignore` 中，但本地 `.env` 文件在 untracked 列表中，需确认

**建议**：
```bash
git config core.autocrlf true  # 确保 Windows 用 CRLF
git add -A
git commit -m "chore: normalize line endings + sync all changes"
git push
```

---

## 二、项目架构总览

```
src/
├── app/
│   ├── page.tsx              # 首页（10 个区块单页）
│   ├── layout.tsx            # 根布局（SEO + JSON-LD + 字体）
│   ├── globals.css           # 深色品牌主题（赤金/炭黑/烟火橙）
│   ├── login/                # 主理人登录页（账号/手机/微信）
│   ├── dashboard/            # 主理人后台（营收大屏+快捷入口）
│   ├── inspect/              # AI 巡店官在线体验
│   ├── chat/                 # AI 智能客服对话页
│   ├── admin/                # 总管理后台（AI 模型配置）
│   ├── about/                # 宣言全文
│   ├── documentary/          # 纪录片页面
│   └── api/
│       ├── auth/login|logout|session  # 认证 API（3 个端点）
│       ├── dashboard/        # 主理人数据 API
│       ├── compass/          # AI 选址罗盘 API（多因子模型）
│       ├── inspect/          # AI 巡店官 API（VLM 视觉分析）
│       ├── briefing/         # AI 经营参谋 API（LLM 生成简报）
│       ├── chat/             # AI 智能客服 API（LLM 多轮对话）
│       ├── packages/recommend # AI 套餐工坊 API（LLM 推荐）
│       ├── admin/models      # AI 模型 CRUD API
│       ├── documentary/      # 纪录片 API
│       └── setup/            # 一键初始化 API
├── components/
│   ├── site/                 # 站点通用组件（header/footer/section-heading）
│   ├── sections/             # 页面区块组件（13 个）
│   └── ui/                   # shadcn/ui 组件库（53 个）
├── hooks/                    # 自定义 hooks（use-reveal/use-count-up/use-mobile）
└── lib/
    ├── db.ts                 # 懒加载数据库客户端
    ├── queries.ts            # 数据访问层（Manager/Session/Revenue/Inspection/Documentary/AIModel）
    ├── llm.ts                # LLM 引擎（DeepSeek API 封装）
    ├── auth.ts ⚠️             # 鉴权工具（与 queries.ts 重复！）
    ├── auth-client.ts        # 客户端鉴权（localStorage + Bearer header）
    ├── session.ts            # 服务端 session 验证
    ├── seed.ts               # 测试数据种子脚本
    └── utils.ts              # 工具函数
```

---

## 三、功能模块实现度评估

### 3.1 首页（面向公众）

| 区块 | 实现状态 | 备注 |
|------|---------|------|
| Hero 宣言区 + 实时数据条 | ✅ 100% | 摊车渐隐背景 + 篝火火焰动画 |
| 序章·飘叔三十年旅程 | ✅ 100% | 时间线组件 |
| 品牌灵魂（三大信仰） | ✅ 100% | |
| 时代背景（4万亿数据） | ✅ 100% | |
| AI 系统五大模块 | ✅ 100% | 含在线体验入口 |
| 套餐介绍 | ✅ 100% | |
| 品牌资产展示 | ✅ 100% | |
| Copywall 社交媒体 | ✅ 100% | |
| 品牌矩阵 | ✅ 100% | |
| 招募表单 | ✅ 100% | 含创始主理人 |

### 3.2 登录后 AI 系统（核心功能）

| 功能 | 实现度 | 可用性 | 详情 |
|------|--------|--------|------|
| **🔐 登录/注册** | ✅ 100% | 🟢 可用 | 账号+手机双通道，scrypt 密码哈希，Bearer Token + Cookie 双鉴权 |
| **📊 主理人 Dashboard** | ✅ 90% | 🟢 可用 | 营收大屏 + 巡店记录 + 全网数据 + 快捷入口 |
| **🧭 AI 选址罗盘** | ⚠️ 70% | 🟡 演示版 | 多因子确定性 PRNG 模型，**正式版需接入高德/百度 LBS** |
| **🔍 AI 智能巡店官** | ✅ 95% | 🟢 可用 | Z.ai GLM VLM 视觉分析，五维评分 + SOP 建议，含完整 Demo 页 |
| **📈 AI 经营参谋** | ✅ 85% | 🟢 可用 | DeepSeek LLM 基于营收数据生成每日简报，含套餐占比分析 |
| **💬 AI 智能客服** | ✅ 90% | 🟢 可用 | DeepSeek LLM 多轮对话，含快捷问题，基于品牌知识库 |
| **🍱 AI 套餐工坊** | ⚠️ 40% | 🟡 仅 API | **API 存在但无前端页面**，无法直接使用 |
| **⚙️ AI 模型配置** | ✅ 95% | 🟢 可用 | 9 个模型预配置，支持启用/禁用/默认切换/密钥管理 |

### 3.3 各 AI 功能详细说明

#### AI 选址罗盘 (`/api/compass`)
- **输入**：地址、场景类型（5种）、经营品类（4种）
- **算法**：多因子叠加模型（人流 34% + 互补 20% + 政策 22% + 竞争逆向 24%）
- **输出**：综合评分(0-100)、预估营收区间、置信度、雷达图、分时段建议
- **底层**：mulberry32 确定性 PRNG（相同输入=相同输出），非真实 LBS 数据
- **状态**：演示用，正式版本需接入地图 API

#### AI 智能巡店官 (`/api/inspect`)
- **输入**：base64 图片（上传或拍照）
- **模型**：Z.ai GLM Vision（通过 z-ai-web-dev-sdk）
- **分析维度**：烤色(28%)、摆盘(20%)、分量(20%)、品牌标识(12%)、卫生(20%)
- **输出**：五维评分、综合分、通过/不通过、亮点/问题列表、SOP 建议、信用分变动
- **兜底机制**：VLM 返回异常时自动降级为默认评估
- **状态**：功能完整，可直接使用

#### AI 经营参谋 (`/api/briefing`)
- **输入**：近14天营收 + 近5条巡店记录
- **模型**：DeepSeek Chat
- **输出**：今日总评、亮点、待改进、明日建议、飘叔寄语
- **需登录**：基于主理人 session
- **状态**：功能完整

#### AI 智能客服 (`/api/chat`)
- **输入**：多轮对话消息
- **模型**：DeepSeek Chat
- **系统提示**：预置品牌知识库（套餐命名、SOP、三大信仰等）
- **状态**：功能完整，`/chat` 页面已就绪但导航不突出

#### AI 套餐工坊 (`/api/packages/recommend`)
- **输入**：天气、温度、时段、近7天销量
- **模型**：DeepSeek Chat
- **输出**：主推套餐、理由、调整建议、备货建议、预计提升
- **状态**：⚠️ **仅有 API，无前端页面！无法直接使用**

---

## 四、代码质量评估

### 🔴 严重问题

1. **API Key 硬编码**（`src/lib/llm.ts:7`）
   ```
   const DEEPSEEK_API_KEY = ... || "sk-d25706cb871f46f289deed77c90fd048";
   ```
   代码中直接暴露 DeepSeek API Key，**必须迁移到环境变量**。虽然 `.env` 在 `.gitignore` 中，但 key 已写入源代码。

2. **代码重复**：`src/lib/auth.ts` 和 `src/lib/queries.ts` 完全重复导出 `hashPassword`、`verifyPassword`、`generateToken`、`SESSION_COOKIE`、`SESSION_MAX_AGE`。且 `src/app/api/auth/login/route.ts` 从两个文件都导入了同名函数。

3. **TypeScript 严格模式关闭**
   - `ignoreBuildErrors: true` — 构建时忽略所有 TS 错误
   - `reactStrictMode: false` — 失去 React 18 双重渲染检测
   - `noImplicitAny: false` — 允许隐式 any

### 🟡 中等问题

4. **登录页硬编码演示账号**：密码 `admin23` 明文展示在页面中
5. **无错误边界**：React 渲染异常会白屏，无兜底 UI
6. **无加载状态骨架屏**：长页面直接渲染，体验可优化
7. **AI 巡店官图片大小限制**(8MB)：客户端限制，但服务端无校验
8. **无请求速率限制**：API 路由可被高频调用
9. **数据库路径硬编码**：`.env` 中使用 `/home/z/my-project/db/custom.db`（Linux 路径，Windows 上需改为本地路径）

### 🟢 优点

10. **Session 管理规范**：使用 `timingSafeEqual` 防时序攻击
11. **懒加载数据库**：Proxy 模式，首次查询才建立连接
12. **SEO 完善**：JSON-LD 结构化数据（Organization/FAQPage/Service/BreadcrumbList）、OpenGraph、Twitter Cards
13. **字体优化**：Noto Sans SC + Serif + Ma Shan Zheng（书法），`preload: false` 优化首屏
14. **UI 组件库规范**：shadcn/ui 53 个组件，有一致的 dark theme token

---

## 五、部署状态

| 项目 | 状态 |
|------|------|
| Vercel 部署 | ✅ 已部署 `tanbot-life.vercel.app` |
| 域名 | ⚠️ `tanbot.life` 已购买，Cloudflare 托管，**等待 DNS 指向 Vercel** |
| 数据库 | ⚠️ Turso 待配置（`DEPLOY.md` 有详细步骤，但 Vercel 环境变量可能未设） |
| 构建 | ✅ `output: "standalone"` 模式，Next.js 16 兼容 |
| SSL | ✅ Vercel 自动 HTTPS |

### 部署验证清单

- [ ] Vercel 环境变量 `DATABASE_URL` 是否已设置 Turso 连接串？
- [ ] 是否已执行 `GET /api/setup` 初始化数据库？
- [ ] `tanbot.life` DNS 是否已 CNAME 到 Vercel？
- [ ] `DEEPSEEK_API_KEY` 是否在 Vercel 环境变量中（不要代码硬编码）？

---

## 六、安全性评估

| 风险 | 严重度 | 描述 |
|------|--------|------|
| API Key 泄露 | 🔴 高 | DeepSeek Key 硬编码在 `src/lib/llm.ts` |
| 演示凭证暴露 | 🟡 中 | 登录页显示默认账号密码 |
| 无 CSRF 防护 | 🟡 中 | 状态变更端点缺少 CSRF token |
| 无速率限制 | 🟡 中 | API 端点可被滥用 |
| SQL 注入 | 🟢 低 | 使用参数化查询，风险可控 |
| XSS | 🟢 低 | React 默认转义 + 使用 dangerouslySetInnerHTML 的 JSON-LD 是静态数据 |

---

## 七、下一步完善建议

### 🔴 立即修复（P0）

1. **移除硬编码 API Key**：将 `sk-d25706cb871f46f289deed77c90fd048` 迁移到环境变量 `DEEPSEEK_API_KEY`
2. **同步 GitHub**：解决 CRLF 问题，push 所有未提交修改
3. **删除重复代码**：删除 `src/lib/auth.ts` 中的重复函数，统一使用 `queries.ts`
4. **修复 DATABASE_URL**：Windows 本地开发使用 `file:./db/custom.db`

### 🟡 近期完善（P1）

5. **AI 套餐工坊前端**：创建 `/packages` 页面，接入已有 API
6. **补全导航**：在 SiteHeader 中增加 `/chat`、`/inspect`、`/documentary` 入口
7. **打开 TypeScript 严格检查**：修复类型错误后启用 `noImplicitAny: true`
8. **Vercel 环境变量**：配置 `DATABASE_URL`、`DEEPSEEK_API_KEY`
9. **开启 `reactStrictMode: true`**：检测副作用问题
10. **Cloudflare DNS 配置**：将 `tanbot.life` 指向 Vercel

### 🟢 功能增强（P2）

11. **真实 LBS 数据接入**：集成高德/百度地图 API 替代选址罗盘的 mock 数据
12. **营收/巡店数据上报 API**：主理人可通过 Dashboard 提交每日数据
13. **PWA 支持**：添加 `manifest.json` + Service Worker，使摊车主可在手机上安装
14. **消息推送**：巡店不达标时 Web Push 通知主理人
15. **数据导出**：主理人可导出营收/巡店 CSV
16. **多语言支持**：英文版（`/en` 路由已在 metadata 中预留）
17. **错误监控**：集成 Sentry 或 Vercel Analytics
18. **图片持久化存储**：使用 Vercel Blob 或 Cloudflare R2 存储巡店照片

---

## 八、总结

项目整体完成度约 **80-85%**。核心业务逻辑和 AI 功能框架已经就绪，是一个可以在 demo 层面完整运行的系统。主要短板集中在三个方面：

1. **安全配置**（硬编码 Key、重复代码、TypeScript 宽松模式）
2. **部署就绪度**（Turso 数据库未配置、域名未指向）
3. **功能补齐**（AI 套餐工坊缺前端、数据上报未实现、缺少 PWA）

在修复上述 P0/P1 问题后，系统即可进入实际的线上验证阶段。

---

> *审计生成时间: 2026-07-26 21:50 CST*  
> *审核模型: lobsterai-server/deepseek-v4-pro*
